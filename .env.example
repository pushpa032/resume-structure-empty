# Example env variables to use in production. Replace values and add via Vercel UI or `vercel env` CLI.

# OpenAI API Key (if you integrate AI analysis on backend)
OPENAI_API_KEY=your-openai-api-key-here

# App mode: 'development' or 'production' (not required but sometimes used)
NODE_ENV=production

# Custom settings or third-party secret keys
# EXAMPLE: ANALYTICS_KEY=
